# cloudflared - Argo Tunnel Client

Currently used for DNS-over-HTTPS. The upstream Dockerfile is more geared
towards building and runs on AMD64, which is why a custom one is implemented
using the instructions [from Pi-hole's
website](https://docs.pi-hole.net/guides/dns-over-https/).

